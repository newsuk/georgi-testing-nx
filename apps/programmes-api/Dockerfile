# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build nx-demo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nx-demo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system programmes-api && \
          adduser --system -G programmes-api programmes-api

COPY dist/apps/programmes-api programmes-api/
RUN chown -R programmes-api:programmes-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix programmes-api --omit=dev -f install

CMD [ "node", "programmes-api" ]
